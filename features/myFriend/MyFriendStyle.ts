import { StyleSheet } from 'react-native'


const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    search: {
        alignItems: 'center',
        marginTop: 50,
    }
})

export default styles